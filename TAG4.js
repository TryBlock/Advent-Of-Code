const input = require("./input");
const lines = input.getLines("./tag4.txt");

const getRandomNumbers = () => {
  return lines[0];
};

const getBoards = () => {
  const boards = [];
  let temp = [];
  lines.slice(2).forEach((line) => {
    
  });
  return boards;
};

console.log(getRandomNumbers());
console.log(getBoards());
